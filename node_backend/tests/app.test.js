import { test } from 'node:test'; // Import the `test` module
import assert from 'node:assert'; // Import the `assert` module for assertions

test('Dummy test', async () => {
  assert.strictEqual(2, 2);
});
